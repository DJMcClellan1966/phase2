# phase2
Phase 2: Dynamic Corpus + Traceable Local Agent  Explore a **local-only, non-hallucinatory, traceable, grounded** agent (dictionary-style) using a **corpus that grows and changes based on need** — a large store that is not necessarily loaded in full every time.
